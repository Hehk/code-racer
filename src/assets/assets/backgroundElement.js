const content =
`import React from 'react';
import './style.css';

const Background = (props) => (
  <div className="background" {...props}></div>
);

export default Background;`;

export default {
  source: 'this app',
  content,
};
